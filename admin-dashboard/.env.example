# Admin Dashboard (Frontend) Environment Example
# Values are injected at build time by Vite (must start with VITE_)
# Default login seed (optional convenience only; remove in production)

VITE_API_BASE_URL=/api

# Base URL for admin backend API (should point to /api root, not including /admin)
VITE_ADMIN_API_BASE=http://localhost:3001/api

# Must match ADMIN_API_KEY in admin-backend/.env (used for x-admin-key header)
VITE_ADMIN_API_KEY=yourStrongKeyHere

    